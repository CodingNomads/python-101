# When you run the code below, you'll get SyntaxErrors.
# The variable names are all messed up and don't follow the rules!
# Fix the variable names so the code works as expected.

# 1stvariable = 40      # Cannot start with a number
# second variable = 2   # Cannot contain a space

# finally = 1stvariable + second variable  # Cannot be a Python keyword
# print(finally)


firstvariable = 40
second_variable = 2

then_finally = firstvariable + second_variable
print(the_finally)